
* How we upgrade rust version? How to use multiple versions of rust locally? like we do for node using nvm 
* panic? Is there a global event hadler to log before the program exists. process.on('uncaughtException')
* Zero-cost abstractions ?
* What editor you use? Which is better?
* Web Assembly WASM

build.rs is a special file in rust that runs when we execute caro run.

Virtual thread in Java ?

